# Server Environment Variables
# ⚠️ SECRETS - NEVER commit real values
# Copy this file to .env and fill in real values

# Server
NODE_ENV=development
PORT=5001

# MongoDB
MONGODB_URI=mongodb://localhost:27017/youth_portal

# JWT (if needed for additional auth)
JWT_SECRET=

# Firebase Admin SDK (REQUIRED for token verification)
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

# AWS S3 (REQUIRED for media uploads)
# Credentials come from IAM role in App Runner, or AWS CLI locally
AWS_REGION=ap-south-1
S3_BUCKET_NAME=youth-portal-media
